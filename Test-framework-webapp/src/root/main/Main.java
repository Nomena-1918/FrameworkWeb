package root.main;

import root.classesTest.Emp;
import root.classesTest.Empmodel_plat;
import utilPerso.FileUpload;

import java.sql.Date;
import java.time.Instant;
import java.time.LocalDate;

public class Main {
    public static void main(String[] args) throws Exception {
        System.out.println("\n\tTest-framework-webapp built successfully ! 🚀\n\n");
/*
        Emp emp = new Emp();
        emp.setId(11);
        emp.setDate(Date.valueOf(LocalDate.now()));
        emp.setIdEmp(1);
        emp.setIdPlat(3);
        emp.setFichier(new FileUpload("platTest.txt", new byte[3]));

        Empmodel_plat e = new Empmodel_plat(emp);
        e.save(null);
 */
    }
}
